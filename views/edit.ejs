<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <h1>Edit Profile</h1>
    <form action="/profile/update" method="POST">
        <!-- Move Email input above Bio -->
        <label>Email:</label>
        <input type="email" name="email" value="<%= user.email %>" required>
        <br>

        <label>Bio:</label>
        <textarea name="bio"><%= user.bio %></textarea>
        <br>

        <label>Favorite Teams:</label>
        <input type="text" name="favoriteTeams" placeholder="Comma-separated teams" value="<%= user.favoriteTeams.join(', ') %>">
        <br>

        <button type="submit">Save Changes</button>
    </form>

    <!-- Cancel button to return to profile without changes -->
    <a href="/profile"><button>Cancel</button></a>
</body>
</html>


